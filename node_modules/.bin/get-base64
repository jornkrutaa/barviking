#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../get-base64/cli.js" "$@"
  ret=$?
else 
  node  "$basedir/../get-base64/cli.js" "$@"
  ret=$?
fi
exit $ret
